from setuptools import find_packages, setup

setup(
    name='dictify_maya_meshes',
    packages=find_packages(include=['dictify_maya_meshes']),
    version='0.0.1',
    description='Turns Maya mesh data into a list of dicts',
    author='Aleksandar Ignjatovic'
)